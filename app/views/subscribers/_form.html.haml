= form_for @subscriber do |f|
  - if @subscriber.errors.any?
    #error_explanation
      %h2= "#{pluralize(@subscriber.errors.count, "error")} prohibited this subscriber from being saved:"
      %ul
        - @subscriber.errors.full_messages.each do |msg|
          %li= msg

  .row
    .col-md-7
      .form-group
        = f.text_field :email, placeholder: "Enter email address"
    .col-md-5
      = f.submit 'Reserve Your Spot', :class => "btn btn-danger btn-lg"
      %p.text-muted.pam
        Beta invites to be sent out soon.
